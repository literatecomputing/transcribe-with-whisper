# Core dependencies
pydub
webvtt-py
pyannote.audio
huggingface_hub
torch       # CPU-only by default
faster-whisper

# Web server dependencies (for enhanced progress tracking)
fastapi
uvicorn[standard]
python-multipart
